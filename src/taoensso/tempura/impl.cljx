(ns taoensso.tempura.impl
  "Private implementation details"
  #+clj  (:require [clojure.set :as set])
  #+cljs (:require [clojure.set :as set])
  #+cljs (:require-macros
          [taoensso.tempura.impl :as impl-macros
           :refer ()]))
